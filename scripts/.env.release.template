# App Store Connect API Key credentials
# Copy this file to .env.release and fill in your values.
# .env.release is gitignored — do NOT commit it.
#
# To get these values:
# 1. Go to https://appstoreconnect.apple.com/access/integrations/api
# 2. Create a new key with Admin or App Manager role
# 3. Download the .p8 file and note the Key ID + Issuer ID

ASC_KEY_ID=YOUR_KEY_ID_HERE
ASC_ISSUER_ID=YOUR_ISSUER_ID_HERE
ASC_KEY_PATH=~/.private_keys/AuthKey_YOUR_KEY_ID.p8
